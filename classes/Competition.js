class Competition {
    constructor(startlist, starting_interval, starting_hour) {
        this.startlist = startlist; // un ensemble de noms + numéro de dossard
        this.starting_interval = starting_interval; 
        this.starting_hour = starting_hour; 
        this.starts_orders = undefined; 
    }

    
}